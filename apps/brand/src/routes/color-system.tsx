import { createFileRoute } from "@tanstack/react-router"
import { SlideDivider } from "../deck/layouts/SlideDivider"

export const Route = createFileRoute("/color-system")({ component: SlideDivider })
