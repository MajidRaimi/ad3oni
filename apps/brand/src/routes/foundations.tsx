import { createFileRoute } from "@tanstack/react-router"
import { SlideDivider } from "../deck/layouts/SlideDivider"

export const Route = createFileRoute("/foundations")({ component: SlideDivider })
